let a=40;
switch(true)
{
    case (a>=90 && a<=100):
        console.log("grade A");
        break;
    case (a>=80 && a<=90):
        console.log("grade B");
        break;
    case (a>=70 && a<=80):
        console.log("grade  c");
        break;
    case (a>=60 && a<=70):
        console.log("grade D");
        break;
    default:
        console.log("grade E");
}